# CI/CD Workflows

This directory contains all GitHub Actions workflows for the ApexSigma DevEx Pipeline (ADEP).

- `ci.yml`: The core Continuous Integration workflow that runs on every push to a feature branch. It includes jobs for linting, testing, and build validation.
